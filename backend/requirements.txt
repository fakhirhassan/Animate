# Flask and extensions
Flask==3.0.0
Flask-CORS==4.0.0
python-dotenv==1.0.0
werkzeug==3.0.1
requests>=2.31.0

# Image processing
Pillow==10.1.0
opencv-python==4.8.1.78

# Scientific computing
numpy==1.26.2
scipy==1.11.4

# Deep learning (PyTorch)
# NOTE: Install CUDA version manually FIRST:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
# Then install the rest of this file. Do NOT let pip install the CPU torch from here.
torch>=2.2.0
torchvision>=0.17.0

# 3D processing
trimesh==4.0.5
open3d>=0.18.0
pyglet<2

# Depth estimation models
timm==0.9.12
transformers>=4.36.0

# Video generation (Wan2.1 / Wan2.2)
diffusers>=0.33.0
accelerate>=1.0.0
safetensors>=0.4.0
huggingface_hub>=0.23.0

# TTS (Kokoro-82M)
# NOTE: Do NOT install mlx-audio — it is Apple Silicon only and will fail on Windows/CUDA.
# kokoro works on Windows/CUDA without mlx.
kokoro>=0.9
soundfile>=0.12.0

# Scene parsing (Ollama LLM — install Ollama separately from https://ollama.com/download)
ollama>=0.4.0

# Video stitching (FFmpeg must be installed separately)
# Windows: winget install Gyan.FFmpeg
# Then restart terminal so ffmpeg is in PATH

# Database
psycopg2-binary==2.9.9

# Production server
gunicorn==21.2.0
